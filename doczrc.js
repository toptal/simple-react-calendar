import { css } from 'docz-plugin-css'

export default {
  title: 'SimpleReactCaledar',
  description: 'A simple calendar component for React based applications.',
  typescript: true,
  codeSandbox: false,
  plugins: [
    css({
      preprocessor: 'stylus',
    })
  ]
}
